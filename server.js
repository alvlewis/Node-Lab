var msg = require('./quotes.js');




"use strict";
const http = require("http");
http.createServer((req, res) => {
    res.writeHead(200, {"Content-type": "text/plain"});
    res.write(

        raisedByMen = msg[Math.floor(Math.random()*msg.length)]

);        //WRITE

res.end();
}).listen(3000);